import styles from "../styles/NavBar.module.css";

function NavBar () {
    return (
            <div>
                <nav>
                    
                </nav>
            </div>
    );
}

export default NavBar;