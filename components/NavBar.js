import styles from "../styles/NavBar.css";

function NavBar () {
    return (
            <div>
                <nav>
                    
                </nav>
            </div>
    );
}

export default NavBar;